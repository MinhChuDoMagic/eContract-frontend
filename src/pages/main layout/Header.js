import { Box } from "@mui/material";

export const Header = () => {
  return(
    <Box sx={{
        position: "absolute",
        top: 0,
        left: 0,
        width: "100%",
        height: "20vh",
        backgroundColor: "GrayText",
    }}>

    </Box>
  );
};